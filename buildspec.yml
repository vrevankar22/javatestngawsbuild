version: 0.2

phases:
  install:
    commands:
      - rm -f /usr/local/bin/chromedriver || true
      - google-chrome --version || true
      - java -version
      - mvn -version

  build:
    commands:
      - mvn clean test